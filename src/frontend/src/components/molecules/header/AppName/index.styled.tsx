import { styled, theme } from '@/styles'

export const Wrapper = styled.div`
  display: block;
  padding: 5px 20px;
  border-radius: 5px;
  font-size: 100px;
  font-weight: 800;
  color: ${theme.color.white};
`
